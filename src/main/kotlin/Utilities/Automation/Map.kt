package Utilities.Automation

class Map(var activities: List<Activity>) {
    val obstacles = mutableListOf<Obstacle>()
    val landmarks = mutableListOf<Landmark>()

    fun display() {
        // look at the CameraServer API
    }

}